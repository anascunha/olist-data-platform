def validate_schema(query: str, schema: dict) -> bool:
    """
    Simula a validação semântica de uma query contra um esquema.
    Verifica se colunas e tabelas referenciadas existem.
    """
    # pseudo validação
    # if "customer_id" not in schema:
    #     raise Error("Coluna não existe")
    pass
